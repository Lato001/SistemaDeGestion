/// -Eliminar ncomprasrealizadas (ya comente todas las líneas donde aparece para que no se ejecuten, luego hablamos entre todos de que hacer con esto)

/// -sumar sueldo a empleado

/// -En la clase venta en cuanto a los datos del empleado y del cliente solo nos debe devolver sus ID's o DNI. Algo con lo que los podamos identificar.

/// -Quitar variables ausentes, vacaciones, diasTrabajados y agregar asistencias

/// -Agregar base de datos con los productos/articulos que vamos a vender

/// -Eliminar ncomprasrealizadas (ya comente todas las líneas donde aparece para que no se ejecuten, luego hablamos entre todos de que hacer con esto)

/// -sumar sueldo a empleado

/// -Crear un menú por clase, es decir, la clase persona necesita un menú donde llamemos a sus funciones, la clase empleado, venta y cliente, lo mismo.
///Luego llamar en un menú principal a todos estos menús

///Todos los cambios realizados en el codigo se deben actualizar en el diagrama.


/// __IMPORTANTE_________

///Cambios hechos:
///Cambios de los atributos de persona de tipo string a tipo char en el diagrama
///Cambio de nombre de la clase Comprador a Cliente
///Se agrego nTelefono como atributo a persona.

///EN PROCESO
/* -En la clase venta en cuanto a los datos del empleado y del cliente solo nos debe devolver sus ID's o DNI. Algo con lo que los podamos identificar. */

/// __IMPORTANTE_________
/// __IMPORTANTE_________
/// __IMPORTANTE_________
/// __IMPORTANTE_________
/// __IMPORTANTE_________
/// __IMPORTANTE_________
/// __IMPORTANTE_________
